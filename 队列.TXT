
    队列是一种特殊的线性表，特殊之处在于它只允许在表的前端进行删除操作，而在表的后端进行插入操作，和栈一样，队列是
一种操作受限制的线性表。进行插入操作的端称为队尾，进行删除操作的端称为队头。队列中没有元素时，称为空队列。
    队列的数据元素又称为队列元素。在队列中插入一个队列元素称为入队，从队列中删除一个队列元素称为出队，因为队列只允许
在一端插入，在另一端删除，所以只有最早进入队列的元素才能最先从队列中删除，故队列又称为先进先出线性表。

线性表的操作包括：
1.清空队列
2.判断是否为空
3.元素的个数
4.入队列
5.出队列
6.取对头元素 

建立接口
1.定义抽象类型
2.声明接口函数

    链队列中各个数据的存储空间可以不连续，基于链表的队列，链表头结点代表的是链表头，为了方便添加节点操作而定义。
不携带有效的应用数据。其后的节点才是有效节点。
入队列即将新元素添加至链表尾部，出队列就是移除第一个数据节点。这些操作在链表中都已有相应的接口。因此给予之前的链表程序，实现链表队列
非常容易。在队列结构体中，仅需包含链表头节点，无需front、rear成员‘’。
