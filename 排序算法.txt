输入：N个待排序的数组成的数组
输出：按顺序从小到大排列好的数组。

1.睡眠排序
  新建N个线程，与N个数一一对应，初始化后，线程开始睡眠，睡眠对应的数的时间各自醒来并输出对应的数。这样，
最小的数对应的线程最先醒来，这个数最早被输出。等所有线程都醒来，排序就结束了。

注意：此算法缺陷，数据不能离散性太大，比如：1~2^32，这样会导致睡眠时间太长，线程无法快速完成。
     不能处理负数。

